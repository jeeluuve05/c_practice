#include <stdio.h>

// Calculate and print the sum of integers from 1 to 10
int main() {
    int sum = 0;
    for (int i = 1; i <= 10; i++) {
        sum += i;
    }
    printf("Sum of integers from 1 to 10 is %d\n", sum);
    return 0;
}
